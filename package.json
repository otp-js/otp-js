{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build --parallel",
    "watch": "lerna run watch --parallel"
  },
  "devDependencies": {
    "@babel/cli": "^7.14.5",
    "@babel/core": "^7.14.6",
    "@babel/preset-env": "^7.14.7",
    "lerna": "^4.0.0"
  }
}
